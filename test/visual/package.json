{
  "name": "sbnn-visual-tests",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Browser rendering tests for the sbnn review UI. Separate from web/ so the shipped bundle keeps no test dependencies.",
  "scripts": {
    "test": "playwright test",
    "list": "playwright test --list",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "1.56.0"
  }
}
