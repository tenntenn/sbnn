/sbnn
node_modules/
.DS_Store
*.log

# gogo の作業用ディレクトリ（進捗ダッシュボードの状態・計測ログ・スナップショット）。
#
# ディレクトリごと無視すると、中を個別に追跡へ戻せない（git の仕様）。
# 計画担当が書いた指示文だけは残さなければならないので、中身を無視して
# orders/ だけを追跡へ戻す。
.gogo/*
!.gogo/orders/

# worktree の持ち主を書くファイル（gogo のハーネスが見る）
.gogo-owner
