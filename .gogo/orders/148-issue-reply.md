slug: issue-reply

# 指示文 148 — 前提が事実と違う issue への返信 (#17 #19 #48 #54 #110 #148)

- 対応 issue: #17 / #19 / #48 / #54 / #110 / #148
- ダッシュボードのタスク ID:
  `t-85b44f` (#17) / `t-89ff6d` (#19) / `t-1267ee` (#48) / `t-6cea53` (#54) /
  `t-10c7a9` (#110) / `t-650017` (#148)
- 作業者: 1 名
- 優先度: 高（他レーンの PR がマージされる前に返信を置きたい）
- 期限: 2026-08-25 中
- 共通規約: `/home/user/briefs/COMMON.md`。ただし**下の「この指示文の性質」が優先する。**

## この指示文の性質 — ソースを 1 行も変更しない

**触ってよいのは GitHub の issue コメントだけ。**

- **担当ファイル: なし。**リポジトリのファイルを 1 つも変更しない。
- **worktree を使わない。ブランチを切らない。コミットしない。push しない。PR を作らない。**
  したがって slug から決まる `.gogo/wt/issue-reply` / `gogo/issue-reply` は**作らない**。
- 使うツールは `mcp__github__issue_read` /
  `mcp__github__add_issue_comment` / `mcp__github__issue_write` の 3 つだけ。
- リポジトリ (`/home/user/sbnn`) は**読むだけ**。行番号の裏取りに `grep` するのは構わない。

ファイルを 1 つも触らないので、他のどの指示文とも担当ファイルは重ならない。

## 判断はすべて確定済み。覆さない

下の 6 件の「実装するか」「閉じるか」は**秘書が確定させたもの**である。
作業者は**内容を蒸し返さない**。やることは、確定済みの判断を、
**根拠（ファイルと行）を添えて issue に書くこと**だけ。

判断に迷って止まらない。迷ったら下に書いてある既定のとおりに実行し、
迷った点を報告に書く。

## 全件に共通する書き方

- **本文は英語で書く。** issue も PR もすべて英語なので、それに合わせる。
- 根拠は必ず**ファイル名と行番号**で示す。「〜のあたり」と書かない。
  行番号は `main` の `7aa2d78` 時点のもの。下に確認済みの値を載せてあるので、それを使う。
- モデル名（Opus / Claude / AI 等）を本文に書かない。下の固定フッタだけが例外。
- **コメント本文の末尾に、必ず次をそのまま入れる**（空行 → `---` → 斜体 1 行）:

```

---
_Generated by [Claude Code](https://claude.ai/code)_
```

- 閉じるときは `mcp__github__issue_write` を使い、
  `method=update`, `state=closed`, **`state_reason` を必ず設定する。**
- **`state_reason` は 5 件とも `completed` を使う。`not_planned` は使わない。**
  理由: 下の 5 件はどれも「対応しない」のではなく、**すでに直っていて PR が出ている**。
  提案どおりの形ではないものはあるが、issue が指した不具合そのものは解消されている。
  `not_planned` で閉じると記録が事実と食い違う。
- **順序を守る。まずコメントを投稿し、投稿が成功してから閉じる。**
  先に閉じるとコメントが付かないまま閉じた issue が残りうる。

## 1 件ずつ

### #17 — Combined diffs: addition/deletion counts are wrong

- **実装しない。返信して閉じる（`completed`）。**
- 事実: `diff-parse` レーンが #16 と #17 を **PR #204** に統合済み（open・未マージ）。
- 返信に入れる根拠:
  - #17 の本文自身が `Fix follows from fixing the marker columns.` と書いている。
    つまり出題者も派生であることを認めている。
  - `internal/diff/parse.go:336` の `readCombinedHunk` が本体の欠陥。
    combined diff は親ごとにマーカー列を持つのに、各行の先頭 1 文字しか見ていなかった。
  - **カウント側に独立したバグは無い。** `internal/diff/parse.go:420` の `finalize` は
    行の `Kind` を数えているだけで、独自の算術を持たない:

    ```go
    for _, h := range f.Hunks {
        for _, l := range h.Lines {
            switch l.Kind {
            case model.LineAdd:
                f.Additions++
            case model.LineDelete:
                f.Deletions++
            }
        }
    }
    ```

    したがってマーカー列を正しく読めば件数は自動的に正しくなる。
  - #204 は #17 の再現例（`@@@ -1,2 -1,2 +1,2 @@@` の 3 行ハンク）を
    テストに取り込み、`+1 -1` になることを明示的に検査している。
  - 2 本に割ると `readCombinedHunk` の同じ行を触る PR が 2 本になるため 1 本にまとめた。
- 「Fixed by #204」と明記して閉じる。

### #19 — `shorten()` cuts UTF-8 mid-rune

- **実装しない。返信して閉じる（`completed`）。**
- 事実: **PR #198** が実装済み（open・未マージ）。`shorten` はルーン単位で数えるようになった。
- 返信に入れる根拠:
  - 元の欠陥は `internal/server/fold.go:120-126`。`return s[:n-1] + "…"` がバイトで切っていた。
  - **issue の再現例そのものは、修正後には切り詰めが起きない。**
    再現例の文字列は **118 バイトだが 43 ルーン**しかないため、
    `n` をルーンとして数えると 60 に収まり、切られずそのまま返る。
    つまり指定された切り詰め長 60 が、再現文字列の実長（43 ルーン）より長かった。
    これは修正が誤りなのではなく、**再現例の数字が緩かった**ということ。
  - #198 は両方をテストしている。`shorten(s, 60)` は無傷で返ること、
    `shorten(s, 20)`（本当に長すぎる場合）は valid UTF-8 で
    ちょうど 20 ルーン・末尾が省略記号になること。どちらも修正前は落ちる。
  - issue が併記していた「表示幅で切るほうがなお良い」は
    `golang.org/x/text` の追加が要るため見送り。`go.mod` はこのレーンの担当外。
    必要なら別 issue に切る、と書く。
- 「Fixed by #198」と明記して閉じる。

### #48 — `sbnn hook --clear --on-review '...'` silently ignores the registration

- **実装しない。返信して閉じる（`completed`）。**
- 事実: **PR #206** が実装済み（open・未マージ）。**ただし issue の提案どおりではない。**
- 返信の骨子は「直した。ただし提案どおりの形ではない。理由はこう」。
- 返信に入れる根拠:
  - 元の欠陥は `cmd/hook.go:75` の `case hookClear:` が `switch` の先頭にあり、
    そこで `return nil` していたこと。
  - issue が挙げた 2 案のうち **排他** を採った。
    `--clear` に「消す」と「置き換える」の 2 つの意味を持たせると、
    他のフラグの有無で意味が変わることになり、スクリプトから扱いづらい。
  - **issue が書いた `MarkFlagsMutuallyExclusive("clear", "on-review", "on-review-url")`
    はそのまま採れない。** 3 つを 1 グループにすると
    `--on-review` と `--on-review-url` の同時指定まで禁じてしまうが、
    これは**現に動いている正当な組み合わせ**である。
    `runHook` は両方を 1 つの `model.Hook{Command: ..., URL: ...}` として `AddHook` に渡し、
    `describeHook` にはその形専用の分岐（`"%s (and POST %s)"`）がある。
  - そこで 2 つのペアに分けた:

    ```go
    hookCmd.MarkFlagsMutuallyExclusive("clear", "on-review")
    hookCmd.MarkFlagsMutuallyExclusive("clear", "on-review-url")
    ```

    報告された不具合は直り、正当な組み合わせは残る。
  - issue 末尾の「root コマンドにも同じ形がある」は **#49** として別に扱っており、
    `cmd/root.go` は #206 では触っていない、と書く。
- 「Fixed by #206」と明記して閉じる。

### #54 — `sbnn comment --json --suggest -` both read stdin

- **実装しない。返信して閉じる（`completed`）。**
- 事実: **PR #185** が実装済み（open・未マージ）。**ただし issue の提案どおりではない。**
- 返信の骨子は #48 と同じく「直した。ただし提案どおりではない。理由はこう」。
- 返信に入れる根拠:
  - 元の欠陥は `cmd/comment.go:109` の `readBulkComments(os.Stdin)` と
    `cmd/comment.go:154` の `suggestionText()` が両方 stdin を読むのに、
    どちらが勝つかが `if` の順序でしか決まっていなかったこと。
  - **issue が書いた
    `MarkFlagsMutuallyExclusive("json", "suggest", "suggest-file", "message")`
    はそのまま採れない。** 4 つを 1 グループにすると
    `--message` と `--suggest` の同時指定まで禁じてしまうが、
    これは**コマンド自身のヘルプに載っている正当な使い方**である:
    `sbnn comment README.md:12-18 -m "Reworded" --suggest "$(cat new.md)"`。
  - そこで 3 つのペアに分けた（`json`×`message` / `json`×`suggest` /
    `json`×`suggest-file`）。stdin の奪い合いと黙って捨てられるテキストだけを禁じ、
    通常の単一コメントはそのまま通る。
  - issue が指摘した「どの単一コメント用フラグが bulk のエントリに効くのか
    ヘルプから読めない」も #185 で対応済み。
    `--author` / `--diff` / `--question` が、
    `author` / `diffId` / `question` を省いたエントリの既定値として効くこと、
    side はエントリ側だけから取ることをヘルプに明記した。
- 「Fixed by #185」と明記して閉じる。

### #110 — The skill documents the `comments --format json` shape wrongly

- **実装しない。返信して閉じる（`completed`）。**
- 事実: **PR #232** が実装済み（open・未マージ）。`skills/sbnn/SKILL.md` のみ変更。
- **注意: 「未着手」ではない。`skill-fix` レーンが走り切って PR #232 を出している。**
  返信を書く前に `mcp__github__issue_read` ではなく
  `mcp__github__pull_request_read`（pullNumber=232, method=get）で本文を確認し、
  下の要点と食い違っていないか照合してから書くこと。
- 返信に入れる根拠:
  - issue の指摘（`question` と `suggestions` が `omitempty` なのに
    「every entry has」と書かれている／実在する 5 キーが欠けている）は**正しい**。
    `internal/model/model.go:171` が `Question bool \`json:"question,omitempty"\``、
    `internal/model/model.go:259` が
    `Suggestions []string \`json:"suggestions,omitempty"\`` である。
  - **ただし issue の Expected も 1 点まちがっている。** Expected は `author` を
    「常にあるキー」の側に置いているが、`internal/model/model.go:156` は
    `Author string \`json:"author,omitempty"\`` である。
    ブラウザから付けたコメントは author が空なので、**キーごと存在しない。**
    「空文字が入る」のではない。
  - そこで #232 は測定に従い、`author` を `question` / `suggestions` と同じ
    「設定されているときだけ現れるキー」の側に分類した。
    常にあるキーは 13 個として列挙し、欠けていた
    `group` / `diffId` / `fileId` / `createdAt` / `updatedAt` を補った。
  - #232 は構造体から読んだのではなく、実際に CLI を動かして
    4 種類のコメント（通常 / `--question` / `--suggest` / author 空）の
    キー集合を測って確かめている。
  - issue 末尾の「テストで形を固定したい」は別 issue 扱い。
    #232 は SKILL.md の散文だけを直しており Go コードは触っていない、と書く。
- 「Fixed by #232」と明記して閉じる。

### #148 — The HTTP API is undocumented and unversioned

- **これだけ扱いが違う。実装を続行する。閉じない。**
- **`state` を触らない。`mcp__github__issue_write` を呼ばない。コメントを 1 本足すだけ。**
- 返信の内容は**実数だけ**。設計の議論・今後の方針・`docs/api.md` の目次案などは**書かない。**
- 事実: issue 本文は "the same **20 endpoints** under `/_/api/`" と書いているが、
  実数は違う。`internal/server/server.go:150-173` に登録されているルートを数えると:
  - `/_/api/` 配下 … **23 本**（`server.go:150` から `server.go:172` まで）
  - これに `GET /_/events`（`server.go:173`）を足して … **24 本**
  - `mux.Handle("GET /", s.spaHandler())`（`server.go:174`）は SPA なので数に入れない。
  - `server.go:170` と `server.go:171` は同じ `s.handleDeleteHooks` に向くが、
    **ルートパターンとしては別**（`/hooks` と `/hooks/{id}`）なので 2 本と数える。
- 返信に書くこと（これだけ）:
  - 本文の 20 は実数と合わない。実数は 24 本である。
  - その内訳（`/_/api/` が 23 本 + `GET /_/events` が 1 本）。
  - 数えた場所が `internal/server/server.go:150-173` であること。
  - 作業は実数 24 本で続行していること。
- **閉じない。** issue はそのまま open で残す。

## 完了条件（実行すれば真偽が決まるもの）

- #17 / #19 / #48 / #54 / #110 の 5 件それぞれについて:
  - `mcp__github__issue_read`（method=`get_comments`）で、
    自分が投稿したコメントが**存在する**こと。
  - そのコメント本文の末尾が `_Generated by [Claude Code](https://claude.ai/code)_` で
    終わっていること。
  - `mcp__github__issue_read`（method=`get`）で `state` が `closed` であること。
- #148 について:
  - コメントが**存在する**こと。末尾フッタが付いていること。
  - `state` が **`open` のまま**であること。**ここが closed なら失敗。**
- 全 6 件のコメント URL を報告に列挙すること。
- `git status --short` が、
  この作業による変更を **1 行も出さない**こと（ソース無変更の確認）。

## 守ること

- **ソースを変更しない。** 返信を書く過程で直したくなったコードが出てきても、直さない。
  報告に書く。
- **担当外は触らない。** #16 / #49 など隣接する issue にはコメントしない。
- **判断に迷って止まらない。** 上の既定どおり実行し、迷った点を報告に書く。
- 6 件のうち一部が失敗しても、そこで止まらない。残りを続け、失敗した件を報告に書く。

## 報告に書くこと

冒頭に `slug: issue-reply` と書く。
**branch / worktree / commit は「なし（ソース無変更）」と明記する。**
そのあと次を書く。

```
## 完了
- issue #<N> | comment <URL> | state <closed(completed) | open> | <1 行要約>
...

## 検証
<git status --short の出力、各 issue の state の確認結果>

## 見送り / 疑義
- issue #<N> | <理由>
```
