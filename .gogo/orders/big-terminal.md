slug: big-terminal

# big-terminal — 端末以外の入口をどう並べるかの方針（issue #147）

優先度: P2
期限: 2026-08-25 中（この波の中で）

## 前提（先に読む）

- `/home/user/briefs/COMMON.md` を先に読む。手順・コミット・PR の書式はそこに従う。
- 本体 `/home/user/sbnn` は参照のみ。作業は自分の worktree の中だけ。
- 名前は次の式でだけ決める。自分で別名を付けない。
  - `worktree = /home/user/wt/<slug>`（`<slug>` はこのファイル冒頭の値 = `big-terminal`）
  - `branch   = gogo/issue-147`
- **1 issue = 1 PR。** `origin/main` から切る。
- **push と PR 作成まで行う。マージはしない。**

## この指示文の性質（ここを読み違えると全部やり直しになる）

**実装をまるごと作らない。** issue #147 は、コードを書き始める前に決めるべきことが
決まっていない大きな提案である。この PR で出すのは**方針の文書 1 本**であり、
それに加えてよいのは「あとで方針が変わっても捨てなくて済む、最小の第一歩」だけ。

**本体の実装ファイルは、いま全部どこかのレーンが使用中である。**
`internal/` `cmd/` `web/src/` `main.go` `go.mod` `go.sum` `Taskfile.yml` `.github/`
`aqua.yaml` `README.md` は**1 バイトも触らない。** 触れば必ず衝突する。

## 担当ファイル（これ以外は 1 バイトも触らない）

- `docs/proposals/147-beyond-the-terminal.md`（新規）**これ 1 ファイルだけ**

`web/dist/` は絶対にコミットしない。

## 作るもの: `docs/proposals/147-beyond-the-terminal.md`

`docs/proposals/` はまだ存在しない。このディレクトリを作る最初の PR の 1 つになる。
索引ファイル（`docs/proposals/README.md`）は**作らない**。同じ波で他の 5 本の提案が
並行して書かれており、索引は共有ファイルになって必ず衝突する。

見出しは**次の 7 つを、この綴りのまま、この順で**使う。増やしてよいが減らさない。

```
## 決めること
## 現状（コードを読んで確かめた事実）
## 選択肢
## 決定
## 後戻りしない第一歩
## やらないこと
## 次の 1 PR の範囲
```

各見出しに入れる中身:

- **決めること** — この issue で先に決着させるべき問いを、箇条書きで 2〜4 個。
  「実装するかどうか」ではなく「どちらの形にするか」を問いにする。
- **現状（コードを読んで確かめた事実）** — **必ず実際にファイルを読んで書く。**
  関数名・型名・ファイルパス・行番号を挙げ、短い引用を添える。
  **推測を事実として書かない。** 読んでいないことは書かない。
- **選択肢** — 2 つ以上。それぞれについて「できるようになること」と「払う代償」を書く。
  issue 本文が挙げている案は必ず含める。
- **決定** — **1 つ選ぶ。「どちらもあり得る」で終わらせない。** 選んだ理由を書く。
  ユーザにしか決められない事柄（外部公開・課金・後戻りできない破壊）が含まれるなら、
  そこだけを「ユーザの決めが要る点」として明示し、**残りは決めて先に進める。**
- **後戻りしない第一歩** — 決定がひっくり返っても捨てずに済む、最小の一歩を 1 つ。
- **やらないこと** — この提案の範囲外を明示する。
- **次の 1 PR の範囲** — 次に出す PR が触るファイルと、その完了条件を、
  **そのまま指示文にできる粒度で**書く。

分量は 60 行以上。長さを競わない。**決まっていないことを決めるのが仕事**であって、
issue の言い直しではない。

## この issue で必ず扱うこと

この issue は**それ自体が「まとめて決めよう」という提案**であり、
#125（MCP）・#105（デスクトップ）・#115 / #55（エクスポートのアーティファクト経路）・
エディタ拡張 / LSP を並べている。**この 4 つを個別に設計しない。**
ここで書くのは**順序と、4 つが共通して必要としているものの正体**である。

同じ波で、#125 と #105 の提案文書が**別の作業者によって並行して書かれている。**
それらの中身を待たない。**待つと止まる。** この提案は「順序と共通の前提」を扱い、
個別の設計はそれぞれの提案に委ねる、と本文に明記する。

読んでから書くこと（**実際に開いて確かめる**）:

- `internal/server/` のルート定義 — 「安定した、CLI ではない HTTP API」が
  いまどれだけ揃っているか。issue の最後の主張はここに懸かっている。
- `internal/export/` — エクスポートされたページが何を含むか。
  モバイル経路（#115 / #55）が本当に「経路の決めごと + フォント 1 つ」で済むかを確かめる。
- `cmd/export.go` と `skills/` — スキルがエクスポート経路に案内しているかどうか。
- `internal/model/model.go` の `Comment` — issue の
  「コメントはそのままエディタの診断（diagnostic）の形をしている」という主張の真偽を、
  フィールド単位で確かめる。

「決めること」に必ず含める問い:

1. **順序を確定する。** issue の 1〜4 の並びをそのまま採るのか、変えるのか。決める。
2. **4 つが共通して要求している「安定した HTTP API」を、いつ・どういう形で文書化するか。**
   これがこの提案の中心である。API リファレンスをどこに置き、何を安定と約束するか。
3. **エディタ拡張と LSP のどちらを目指すか。** issue は LSP なら 1 実装で
   複数エディタを覆えると言っている。決める（「まだ決めない」と決めてもよいが、
   その場合は何が分かれば決まるのかを書く）。
4. **やらないと決めるものがあるか。** 4 つ全部をやると約束しない。

「後戻りしない第一歩」の候補: 既存の HTTP API の一覧（パス・メソッド・
リクエスト / レスポンスの形）を提案の中に表としてまとめ、
「これが外向きの契約の候補である」と置く。MCP・デスクトップ・拡張のどれを選んでも捨てずに済む。


## 完了条件（自分で実行して真偽を決められること）

```bash
test -f docs/proposals/147-beyond-the-terminal.md
test $(wc -l < docs/proposals/147-beyond-the-terminal.md) -ge 60
grep -n '^## 決めること' docs/proposals/147-beyond-the-terminal.md
grep -n '^## 現状（コードを読んで確かめた事実）' docs/proposals/147-beyond-the-terminal.md
grep -n '^## 選択肢' docs/proposals/147-beyond-the-terminal.md
grep -n '^## 決定' docs/proposals/147-beyond-the-terminal.md
grep -n '^## 後戻りしない第一歩' docs/proposals/147-beyond-the-terminal.md
grep -n '^## やらないこと' docs/proposals/147-beyond-the-terminal.md
grep -n '^## 次の 1 PR の範囲' docs/proposals/147-beyond-the-terminal.md

# 文書が挙げたコードのパスが全部実在すること（何も返らないのが合格）
grep -oE '(internal|cmd|web|version)/[A-Za-z0-9_./-]+\.(go|ts|tsx|css|json|yaml)' docs/proposals/147-beyond-the-terminal.md \
  | sort -u | while read -r p; do test -e "$p" || echo "MISSING: $p"; done

# 実装ファイルに手を出していないことの証明（何も返らないのが合格）
git diff --name-only origin/main | grep -vE '^docs/proposals/147-beyond-the-terminal\.md$'

go build ./... && go vet ./... && go test ./...
```


## コミット / PR

- 件名は英語・命令形（COMMON.md のとおり）。例: `Propose ...`。
- **フッタは `Refs #147` にする。`Fixes #147` にしない。** 方針文書は issue を閉じない。
  この issue は open のまま残す。
- PR 本文の `## What changed` には、決定の 1 行要約を書く。
- PR 本文の `## Verification` には、上の完了条件のコマンドと結果を貼る。

## 報告に書くこと（**issue へのコメントは書かない。メインが書く**）

1. 何を決めたか（1 行）。
2. **この issue を「過大」または「前提が誤っている」と判断したかどうか。**
   判断したなら、その根拠をコードの引用付きで書く。メインがそれを issue に返信する。
3. ユーザの決めが要る点が残ったなら、その 1 点だけを明示する。
4. `slug` / branch / worktree / commit の 4 つを、この指示文と同じ綴りで。

## 全体を通しての決まり

- 担当外のファイルは触らない。見つけた問題は自分で直さず報告に書く。
- 判断に迷って止まらない。既定を自分で決めて進み、決めた内容と理由を報告に書く。
- 実装を「ついでに」始めない。この PR の価値は、**次の 1 PR がすぐ書けること**にある。
