From cf691e980a8aa032b379ca1e59d4f305f039d862 Mon Sep 17 00:00:00 2001
From: Takuya Ueda <uedatakuya@gmail.com>
Date: Mon, 24 Aug 2026 23:34:19 -1000
Subject: [PATCH] Accept --side spellings that differ only in case or padding
 (#164)

Closes #53.
---
 cmd/comment.go      |  2 +-
 cmd/comment_test.go | 44 ++++++++++++++++++++++++++++++++++++++++++++
 2 files changed, 45 insertions(+), 1 deletion(-)
 create mode 100644 cmd/comment_test.go

diff --git a/cmd/comment.go b/cmd/comment.go
index 21d63bb..5d73e8d 100644
--- a/cmd/comment.go
+++ b/cmd/comment.go
@@ -292,7 +292,7 @@ func readBulkComments(r io.Reader) ([]server.AddCommentRequest, error) {
 }
 
 func normalizeSide(side string) (string, error) {
-	switch side {
+	switch strings.ToLower(strings.TrimSpace(side)) {
 	case "", "new":
 		return "new", nil
 	case "old":
diff --git a/cmd/comment_test.go b/cmd/comment_test.go
new file mode 100644
index 0000000..6b49be3
--- /dev/null
+++ b/cmd/comment_test.go
@@ -0,0 +1,44 @@
+package cmd
+
+import "testing"
+
+func TestNormalizeSide(t *testing.T) {
+	t.Parallel()
+
+	tests := map[string]struct {
+		in      string
+		want    string
+		wantErr bool
+	}{
+		"empty":         {in: "", want: "new"},
+		"new":           {in: "new", want: "new"},
+		"old":           {in: "old", want: "old"},
+		"capitalized":   {in: "New", want: "new"},
+		"upper":         {in: "OLD", want: "old"},
+		"mixed":         {in: "nEw", want: "new"},
+		"padded":        {in: " old ", want: "old"},
+		"padded empty":  {in: "   ", want: "new"},
+		"padded tab":    {in: "\tNEW\n", want: "new"},
+		"unknown":       {in: "left", wantErr: true},
+		"unknown upper": {in: "BOTH", wantErr: true},
+	}
+
+	for name, test := range tests {
+		t.Run(name, func(t *testing.T) {
+			t.Parallel()
+			got, err := normalizeSide(test.in)
+			if test.wantErr {
+				if err == nil {
+					t.Fatalf("normalizeSide(%q) = %q, want an error", test.in, got)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatalf("normalizeSide(%q) returned an error: %v", test.in, err)
+			}
+			if got != test.want {
+				t.Errorf("normalizeSide(%q) = %q, want %q", test.in, got, test.want)
+			}
+		})
+	}
+}
-- 
2.43.0

